<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= products[0].category %></title>
</head>

<body>
    <h1> All <%= products[0].category %></h1>
    <% for(let product of products){ %>
    <h3><%= product.name %></h3>
    <p> price : <%= product.price %> </p>
    <% } %>
</body>

</html>