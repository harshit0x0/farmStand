<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit the product</h1>
    <form action="/products/<%= selectedProduct._id%>?_method=PUT" method="POST">

        <label for="name">Product Name</label>
        <input type="text" name="name" id="name" value="<%= selectedProduct.name %>">

        <label for="price">Product price</label>
        <input type="number" name="price" id="price" value="<%= selectedProduct.price %>">

        <label for="category">Select category</label>

        <% function isOriginal(cat){ %>
        <%    if(cat == selectedProduct.category) return "selected"; %>
        <% } %>

        <select name="category" id="category">
            <option value="fruit" <%= isOriginal("fruit") %>>fruit</option>
            <option value="vegetable" <%= isOriginal("vegetable") %>>vegetable</option>
            <option value="dairy" <%= isOriginal("dairy") %>>dairy</option>
        </select>

        <button>Submit</button>

    </form>
    <a href="/products">back to main</a>
</body>

</html>